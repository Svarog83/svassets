{% extends 'layout.html.twig' %}
{% set active = 'doctrine' %}

{% block content %}

    <h1>{{ 'Doctrine'|trans }}</h1>

    <ul>
        {% for asset in assets %}
            <li>{{ asset.aID }}. {{ asset }}</li>
        {% endfor %}
    </ul>

{% endblock %}
